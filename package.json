{
  "name": "@schemavaults/init-next-app",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "init-next-app": "./dist/index.js",
    "@schemavaults/init-next-app": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.2"
  }
}
